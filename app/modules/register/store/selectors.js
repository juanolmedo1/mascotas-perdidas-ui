export const getUserBasicInformation = ({ registration }) =>
  registration.basicInformation;
export const getUserUbication = ({ registration }) => registration.ubication;
export const getUserLoginData = ({ registration }) => registration.loginData;
export const getProfilePhoto = ({ registration }) => registration.profilePhoto;
